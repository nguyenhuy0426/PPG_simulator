[pytest]
# Only tests/ holds tests. The repository root also carries hardware bring-up
# scripts whose names start with test_ (test_sin_dac.py, test_i2c_speed.py);
# they open a real I2C bus at import time, so collecting them aborts the whole
# run on any machine without the Grove/MCP4725 hardware attached.
testpaths = tests
